# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: homestorage
services:
  app:
    language: dotnet
    project: ./src/HomeStorage.AppHost/HomeStorage.AppHost.csproj
    host: containerapp
